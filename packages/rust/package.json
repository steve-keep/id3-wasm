{
  "name": "rust",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "wasm-pack build --target web --out-dir ../lib/wasm"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}
